# homebrew-loradex

Homebrew tap for the [loradex CLI](https://github.com/keeandrews/loradex-cli).

```bash
brew tap keeandrews/loradex
brew install loradex
```

To upgrade:

```bash
brew update && brew upgrade loradex
```
